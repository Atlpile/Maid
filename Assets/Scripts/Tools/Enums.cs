
public enum E_SoundName
{
    None,
    Run, Jump, Fire, Dash, Hurt,
    AddLimit, GetCoin, Drink,
    BGM1, BGM2
}


public enum E_LimitType { HP, MP, ATK, DEF }
public enum E_BottleType { HP, MP, HMP }
public enum E_TemporaryItemType { Speed, JumpCount, Defence, Pet, AddBullet, Invincible }


public enum E_DestinationTag { Enter, A, B, C }
public enum E_TransitionType { SameScene, DifferentScene }


public enum E_BoxType { Normal, Red, Blue, Yellow, Green }
public enum E_KeyType { RedKey, BlueKey, YellowKey, GreenKey }


public enum E_PlatformType
{
    LeftAndRight, UpAndDown,
    OneWay, DoubleWay,
    DelayDestroy,
}


